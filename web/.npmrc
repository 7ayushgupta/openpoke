# Enforce TLS verification and trust system/corporate CAs exported from macOS keychains
strict-ssl=true
cafile=certs/system-ca-bundle.pem
# Optional: set proxy if required by your network (uncomment and fill)
# proxy=http://your.corporate.proxy:8080
# https-proxy=http://your.corporate.proxy:8080

